export default {raizCuadrada};

/**
 * Calcula la raíz cuadrada de un número. 
 * @param numero Del que se calcula la raíz cuadrada.
 * @returns El resultado de la raíz cuadrada. 
 */
function raizCuadrada (numero : number): number{
    let resultado = Math.sqrt(numero);
    return resultado;


}